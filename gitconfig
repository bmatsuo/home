[user]
	name = Bryan Matsuo
    email = bryan.matsuo@gmail.com

[alias]
    d=diff --color
    sh=show --color
    l=log --oneline -n 5
    ll=log --oneline -n 10
    lll=log --oneline
    ln=l -n
    st=status
    br=branch
    co=checkout
    ci=commit
    cia=commit -a
    gr=log --oneline --graph --color

[core]
	excludesfile = ~/.config/git/gitignore
