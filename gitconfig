[user]
	name = Bryan Matsuo
	email = bmatsuo@janrain.com

[color]
    ui = true

[push]
	default = tracking

[color]
    ui=true

[alias]
    amend=commit --amend
    amendp=commit --amend --patch
    amenda=commit --amend -a
    d=diff
    sh=show
    l=log --oneline -n 5
    ll=log --oneline -n 10
    lll=log --oneline
    ln=l -n
    st=status
    br=branch
    co=checkout
    ci=commit
    cip = commit --patch
    cia=commit -a
    gr=log --oneline --graph
    ff=pull --ff-only
    m=merge --no-ff

[core]
	excludesfile = ~/.config/git/gitignore
