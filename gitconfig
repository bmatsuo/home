[user]
	name = Bryan Matsuo
    email = bryan.matsuo@gmail.com

[alias]
    d=diff --color
    sh=show --color
    l=log --color --oneline -n 5
    ll=log --color --oneline -n 10
    lll=log --color --oneline
    ln=l --color -n
    st=status
    br=branch
    co=checkout
    ci=commit
    cia=commit -a
    gr=log --oneline --graph --color

[core]
	excludesfile = ~/.config/git/gitignore
