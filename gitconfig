[user]
	name = Bryan Matsuo
    email = bryan.matsuo@gmail.com

[push]
	default=tracking

[color]
    ui=true

[alias]
    d=diff
    sh=show
    l=log --oneline -n 5
    ll=log --oneline -n 10
    lll=log --oneline
    ln=l -n
    st=status
    br=branch
    co=checkout
    ci=commit
    cia=commit -a
    gr=log --oneline --graph
    ff=pull --ff-only
    m=merge --no-ff

[core]
	excludesfile = ~/.config/git/gitignore
